"use client";
import {useEffect,useState} from "react";import {onAuthStateChanged} from "firebase/auth";import {getDoc} from "firebase/firestore";import {auth} from "@/lib/firebase";import {Member,memberRef} from "@/lib/user";import {withFirestoreRetry} from "@/lib/firestoreRetry";import {useRouter} from "next/navigation";
export function useMember(){const [member,setMember]=useState<Member|null>(null),[loading,setLoading]=useState(true),router=useRouter();useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){setLoading(false);return router.replace("/login")}try{const s=await withFirestoreRetry(()=>getDoc(memberRef(u.uid)));if(!s.exists()){setLoading(false);return router.replace("/collections")}const d=s.data() as Member;if(d.paymentStatus==="approved"){setMember(d);setLoading(false);return}setLoading(false);if(d.packageId)return router.replace("/payment");return router.replace("/collections")}catch{setLoading(false);router.replace("/login")}}),[router]);return {member,loading}}
