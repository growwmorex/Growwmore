"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {signOut} from "firebase/auth";import {auth} from "@/lib/firebase";import {isAdminEmail} from "@/lib/constants";
const links=[["/dashboard","Overview"],["/dashboard/referrals","Referrals"],["/dashboard/wallet","Wallet"],["/dashboard/withdrawals","Withdrawals"],["/dashboard/profile","Profile"]];
export default function DashboardNav(){const path=usePathname(),router=useRouter(),admin=isAdminEmail(auth.currentUser?.email);return <><nav className="nav"><Link className="brand" href="/dashboard">GROWW<span>MORE</span></Link><div className="navlinks">{admin&&<Link className="btn ghost" href="/admin">Admin OS</Link>}<button className="btn ghost" onClick={()=>signOut(auth).then(()=>router.push("/"))}>Sign out</button></div></nav><div className="dashTabs">{links.map(([href,label])=><Link className={path===href?"active":""} key={href} href={href}>{label}</Link>)}</div></>}
