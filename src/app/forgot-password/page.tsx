"use client";
import {useState} from "react";import {sendPasswordResetEmail} from "firebase/auth";import {auth} from "@/lib/firebase";import Nav from "@/components/Nav";import LuxuryBackground from "@/components/LuxuryBackground";import Link from "next/link";
export default function ForgotPassword(){const [msg,setMsg]=useState(""),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const f=new FormData(e.currentTarget);try{await sendPasswordResetEmail(auth,String(f.get("email")||""));setMsg("Password reset email sent. Check your inbox and spam folder.")}catch(e){setMsg(e instanceof Error?e.message:"Unable to send password reset email.")}finally{setBusy(false)}}
 return <main className="shell"><LuxuryBackground/><Nav/><div className="authWrap"><form className="panel authPanel" onSubmit={submit}><div className="eyebrow">Account recovery</div><h1>Reset your password.</h1><p className="sub">Enter the email used for your Growwmore account. Firebase will send a secure reset link.</p><div className="field"><label>EMAIL</label><input name="email" type="email" required/></div>{msg&&<div className="notice">{msg}</div>}<button className="btn wide" disabled={busy}>{busy?"Sending…":"Send reset link →"}</button><div className="authLinks"><Link href="/login">Back to sign in</Link><Link href="/join">Create account</Link></div></form></div></main>}
