"use client";
import Nav from "@/components/Nav";import LuxuryBackground from "@/components/LuxuryBackground";import {useEffect,useState} from "react";import {onAuthStateChanged} from "firebase/auth";import {auth,db} from "@/lib/firebase";import {doc,getDoc,serverTimestamp,setDoc,updateDoc} from "firebase/firestore";import {memberRef,Member} from "@/lib/user";import {BRAND,PACKAGES,isAdminEmail} from "@/lib/constants";import {useSearchParams,useRouter} from "next/navigation";
export default function Payment(){const q=useSearchParams(),router=useRouter(),[m,setM]=useState<Member|null>(null),[msg,setMsg]=useState(""),pkg=q.get("package")||"";const p=PACKAGES.find(x=>x.id===pkg);
 useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u)return router.push("/login");if(isAdminEmail(u.email))return router.replace("/admin-gateway");if(!u.emailVerified&&!u.providerData.some(x=>x.providerId==="google.com"))return router.replace("/collections");const s=await getDoc(memberRef(u.uid));if(!s.exists())return router.replace("/collections");const d=s.data() as Member;setM(d);if(d.paymentStatus==="approved")router.replace("/dashboard");if(!pkg&&!d.packageId)router.replace("/collections")}),[router,pkg]);
 const selected=p||PACKAGES.find(x=>x.id===m?.packageId);if(!selected)return <main className="shell center">Preparing payment…</main>;
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!auth.currentUser)return;if(!selected)return setMsg("Please choose a valid collection first.");const f=new FormData(e.currentTarget),utr=String(f.get("utr")||"").trim();if(utr.length<6)return setMsg("Enter a valid UTR / transaction reference.");await updateDoc(memberRef(auth.currentUser.uid),{paymentStatus:"pending",packageId:selected.id,packagePrice:selected.price,commission:selected.commission,utr,updatedAt:serverTimestamp()});await setDoc(doc(db,"payments",auth.currentUser.uid),{uid:auth.currentUser.uid,email:auth.currentUser.email,packageId:selected.id,amount:selected.price,utr,status:"pending",createdAt:serverTimestamp()});setMsg("Payment submitted successfully. Founder verification is pending.");setM({...m!,paymentStatus:"pending",packageId:selected.id})}
 return <main className="shell"><LuxuryBackground/><Nav/><div className="paymentWrap"><div className="paymentCard"><div><div className="eyebrow">Secure manual verification</div><h1>{m?.paymentStatus==="pending"?"Verification in progress.":"Complete your bundle payment."}</h1><div className="chosenBundle"><span>{selected.label}</span><h3>{selected.name}</h3><strong>₹{selected.price.toLocaleString("en-IN")}</strong><small>Published referral commission after activation: ₹{selected.commission.toLocaleString("en-IN")}</small></div></div><div className="qrPanel"><img src="/upi-payment-qr.png" alt="Growwmore UPI payment QR code"/><b>Scan with any UPI app</b><span>{BRAND.upiId}</span><span>{BRAND.paymentPhone}</span></div></div>{m?.paymentStatus!=="pending"?<form className="panel paymentForm" onSubmit={submit}><div className="notice">Pay exactly <b>₹{selected.price.toLocaleString("en-IN")}</b>. Then enter your UTR/reference below. Never share your UPI PIN, OTP or banking password.</div><div className="field"><label>UTR / TRANSACTION REFERENCE</label><input name="utr" required placeholder="Enter transaction reference"/></div><button className="btn">Submit payment for approval →</button></form>:<div className="panel pendingPanel"><div className="statusPulse"/><h3>Payment submitted</h3><p>Your dashboard unlocks automatically after founder approval. You can return later using Sign in.</p></div>}{msg&&<p className="success centerText">{msg}</p>}</div></main>}
