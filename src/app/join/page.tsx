"use client";
import Nav from "@/components/Nav";import Link from "next/link";import LuxuryBackground from "@/components/LuxuryBackground";import {useRouter,useSearchParams} from "next/navigation";import {useState} from "react";import {createUserWithEmailAndPassword,sendEmailVerification,updateProfile} from "firebase/auth";import {auth} from "@/lib/firebase";import {isAdminEmail} from "@/lib/constants";import {friendlyAuthError} from "@/lib/authErrors";import {friendlyFirestoreError} from "@/lib/firestoreRetry";import {bootstrapAuthenticatedUser} from "@/lib/authBootstrap";import {prepareAuthPersistence,startGooglePopup} from "@/lib/googleAuthFlow";
export default function Join(){const router=useRouter(),q=useSearchParams(),[err,setErr]=useState(""),[busy,setBusy]=useState(false),referral=q.get("ref")||"";async function google(){setBusy(true);setErr("");try{await startGooglePopup("join",referral)}catch(x){setErr(friendlyAuthError(x));setBusy(false)}}async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr("");const f=new FormData(e.currentTarget);try{await prepareAuthPersistence();const fullName=String(f.get("name")||"").trim(),email=String(f.get("email")||"").trim(),password=String(f.get("password")||""),phone=String(f.get("phone")||"").trim(),ref=String(f.get("ref")||"").trim()||null;const cred=await createUserWithEmailAndPassword(auth,email,password);await updateProfile(cred.user,{displayName:fullName});const result=await bootstrapAuthenticatedUser(cred.user,{fullName,phone,referredBy:ref});if(!isAdminEmail(email)&&!cred.user.emailVerified)await sendEmailVerification(cred.user).catch(()=>undefined);router.replace(result.destination)}catch(x){const raw=x instanceof Error?x.message:String(x||"");setErr(raw.includes("firestore")||raw.includes("database")?friendlyFirestoreError(x):friendlyAuthError(x))}finally{setBusy(false)}}return <main className="shell"><LuxuryBackground/><Nav/><div className="authWrap"><form className="panel authPanel" onSubmit={submit}><div className="eyebrow">Join Growwmore</div><h1>Create your account.</h1><p className="sub">Your bundle is selected on the next page — account creation stays clean and simple.</p><button type="button" className="googleBtn" onClick={google} disabled={busy}><span>G</span> {busy?"Opening Google…":"Continue with Google"}</button><div className="divider"><span>or continue with email</span></div><div className="field"><label>FULL NAME</label><input name="name" required/></div><div className="field"><label>EMAIL</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>PHONE</label><input name="phone" inputMode="tel" required/></div><div className="field"><label>PASSWORD</label><input name="password" type="password" minLength={6} autoComplete="new-password" required/></div><div className="field"><label>REFERRAL CODE / REFERRER ID (OPTIONAL)</label><input name="ref" defaultValue={referral}/></div><p className="microcopy">Email registrations receive a verification email. By creating an account you accept the Terms, Privacy Policy, Referral Terms and No-Refund Policy.</p>{err&&<div className="error">{err}</div>}<div className="authLinks"><Link href="/forgot-password">Forgot password?</Link><Link href="/login">Already a member? Sign in</Link></div><button className="btn wide" disabled={busy}>{busy?"Creating account…":"Create account →"}</button></form></div></main>}
