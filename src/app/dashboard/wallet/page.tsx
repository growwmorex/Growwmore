"use client";
import DashboardNav from "@/components/DashboardNav";import {useMember} from "@/hooks/useMember";
export default function Wallet(){const {member:m,loading}=useMember();if(loading||!m)return <main className="shell"><div className="panel">Loading…</div></main>;const available=Math.max(0,(m.totalEarning||0)-(m.withdrawnAmount||0));
 return <main className="shell"><DashboardNav/><div className="dash"><div className="eyebrow">Wallet</div><h2 className="dashTitle">Earnings overview</h2><div className="stats"><div className="stat"><span>Total earned</span><strong>₹{(m.totalEarning||0).toLocaleString("en-IN")}</strong></div><div className="stat"><span>Paid / withdrawn</span><strong>₹{(m.withdrawnAmount||0).toLocaleString("en-IN")}</strong></div><div className="stat"><span>Available</span><strong>₹{available.toLocaleString("en-IN")}</strong></div><div className="stat"><span>Currency</span><strong>INR</strong></div></div><div className="card" style={{marginTop:20}}><div className="tier">Ledger policy</div><h3>Approved commissions only</h3><p className="sub">Wallet balance reflects commissions credited after admin approval of eligible referred purchases. Paid withdrawals reduce the available balance.</p></div></div></main>}
