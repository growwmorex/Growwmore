"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {allDocs,setMemberState} from "@/lib/admin";import {PACKAGES} from "@/lib/constants";
export default function Members(){const ready=useAdmin(),[rows,setRows]=useState<any[]>([]),[q,setQ]=useState("");async function load(){setRows(await allDocs("members"))}useEffect(()=>{if(ready)load()},[ready]);const f=rows.filter(x=>`${x.fullName} ${x.email} ${x.phone}`.toLowerCase().includes(q.toLowerCase()));if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Member CRM</div><h2 className="dashTitle">Members</h2><div className="field"><input placeholder="Search name, email or phone…" value={q} onChange={e=>setQ(e.target.value)}/></div><table className="table"><thead><tr><th>Member</th><th>Package</th><th>Payment</th><th>Earnings</th><th>Control</th></tr></thead><tbody>{f.map(x=><tr key={x.id}><td>{x.fullName}<br/><small>{x.email}</small></td><td>{PACKAGES.find(p=>p.id===x.packageId)?.name||"—"}</td><td><span className="pill">{x.paymentStatus}</span></td><td>₹{Number(x.totalEarning||0).toLocaleString("en-IN")}</td><td><button className="btn ghost" onClick={async()=>{await setMemberState(x.id,!x.disabled);await load()}}>{x.disabled?"Enable":"Disable"}</button></td></tr>)}</tbody></table></div></main>}
