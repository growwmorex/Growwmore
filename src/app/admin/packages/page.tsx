"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {PACKAGES} from "@/lib/constants";import {allDocs,setPackage} from "@/lib/admin";
export default function Packages(){const ready=useAdmin(),[saved,setSaved]=useState<any[]>([]),[msg,setMsg]=useState("");useEffect(()=>{if(ready)allDocs("packageCatalog").then(setSaved)},[ready]);if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Catalog studio</div><h2 className="dashTitle">Package manager</h2><div className="notice">Editing here writes package overrides to Firestore <b>packageCatalog</b>. Collection is created automatically on first save.</div><div className="grid">{PACKAGES.map(p=>{const o=saved.find(x=>x.id===p.id)||p;return <form className="card" key={p.id} onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await setPackage(p.id,{name:String(f.get("name")),price:Number(f.get("price")),commission:Number(f.get("commission")),active:f.get("active")==="on"});setMsg(`${p.name} saved.`)}}><div className="tier">{p.id}</div><div className="field"><label>NAME</label><input name="name" defaultValue={o.name}/></div><div className="field"><label>PRICE</label><input name="price" type="number" defaultValue={o.price}/></div><div className="field"><label>COMMISSION</label><input name="commission" type="number" defaultValue={o.commission}/></div><label><input name="active" type="checkbox" defaultChecked={o.active!==false}/> Active</label><br/><br/><button className="btn">Save</button></form>})}</div>{msg&&<p className="success">{msg}</p>}</div></main>}
