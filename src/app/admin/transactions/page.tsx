"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {allDocs} from "@/lib/admin";
export default function AdminTransactions(){const ready=useAdmin(),[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(ready)allDocs("commissionTransactions").then(setRows)},[ready]);if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;const total=rows.filter(x=>x.status==="credited").reduce((n,x)=>n+Number(x.amount||0),0);return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Commission ledger</div><h2 className="dashTitle">Transactions</h2><div className="stats"><div className="stat"><span>Entries</span><strong>{rows.length}</strong></div><div className="stat"><span>Total credited</span><strong>₹{total.toLocaleString("en-IN")}</strong></div></div><table className="table"><thead><tr><th>Referrer</th><th>Source member</th><th>Package</th><th>Amount</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.referrerEmail||x.referrerUid}</td><td>{x.sourceEmail||x.sourceMemberUid||"—"}</td><td>{x.packageId||"—"}</td><td>₹{Number(x.amount||0).toLocaleString("en-IN")}</td><td><span className="pill">{x.status}</span></td></tr>)}</tbody></table></div></main>}
