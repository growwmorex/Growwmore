"use client";
import {useEffect,useMemo,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {allDocs} from "@/lib/admin";
export default function AdminTeam(){const ready=useAdmin(),[members,setMembers]=useState<any[]>([]);useEffect(()=>{if(ready)allDocs("members").then(setMembers)},[ready]);const rows=useMemo(()=>members.map(m=>{const children=members.filter(x=>x.referredBy===m.referralCode||x.referredBy===m.email);return {...m,teamSize:children.length,teamEarning:children.reduce((n,x)=>n+Number(x.totalEarning||0),0)}}).sort((a,b)=>b.teamSize-a.teamSize),[members]);if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Network intelligence</div><h2 className="dashTitle">Team members & leaders</h2><table className="table"><thead><tr><th>Member</th><th>Direct team</th><th>Personal earnings</th><th>Team earnings</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.fullName||x.email}<br/><small>{x.email}</small></td><td>{x.teamSize}</td><td>₹{Number(x.totalEarning||0).toLocaleString("en-IN")}</td><td>₹{x.teamEarning.toLocaleString("en-IN")}</td><td><span className="pill">{x.paymentStatus}</span></td></tr>)}</tbody></table></div></main>}
