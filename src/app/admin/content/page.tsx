"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {db} from "@/lib/firebase";import {doc,getDoc,serverTimestamp,setDoc} from "firebase/firestore";
export default function Content(){const ready=useAdmin(),[msg,setMsg]=useState(""),[data,setData]=useState<any>({});useEffect(()=>{if(ready)getDoc(doc(db,"platformContent","home")).then(s=>s.exists()&&setData(s.data()))},[ready]);if(!ready)return <main className="shell"><div className="panel">Loading CMS…</div></main>;async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),values={announcement:String(f.get("announcement")||""),visionTitle:String(f.get("visionTitle")||""),visionBody:String(f.get("visionBody")||""),founderName:String(f.get("founderName")||""),founderNote:String(f.get("founderNote")||""),updatedAt:serverTimestamp()};await setDoc(doc(db,"platformContent","home"),values,{merge:true});setMsg("Homepage CMS saved live.")}return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Content management system</div><h2 className="dashTitle">Homepage CMS</h2><form className="panel inlinePanel" onSubmit={save}><div className="field"><label>ANNOUNCEMENT</label><input name="announcement" defaultValue={data.announcement}/></div><div className="field"><label>VISION TITLE</label><input name="visionTitle" defaultValue={data.visionTitle}/></div><div className="field"><label>VISION BODY</label><textarea name="visionBody" defaultValue={data.visionBody}/></div><div className="field"><label>FOUNDER NAME</label><input name="founderName" defaultValue={data.founderName||"Shamshar Qadri"}/></div><div className="field"><label>FOUNDER NOTE</label><textarea name="founderNote" defaultValue={data.founderNote}/></div><button className="btn">Save live content</button>{msg&&<p className="success">{msg}</p>}</form></div></main>}
