"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {allDocs} from "@/lib/admin";
export default function Finance(){const ready=useAdmin(),[d,setD]=useState<any>({});useEffect(()=>{if(!ready)return;(async()=>{const [p,w,m]=await Promise.all([allDocs("payments"),allDocs("withdrawals"),allDocs("members")]);const approved=p.filter((x:any)=>x.status==="approved"),paid=w.filter((x:any)=>x.status==="paid");setD({sales:approved.reduce((a:number,x:any)=>a+Number(x.amount||0),0),payouts:paid.reduce((a:number,x:any)=>a+Number(x.amount||0),0),earnings:m.reduce((a:number,x:any)=>a+Number(x.totalEarning||0),0),salesCount:approved.length})})()},[ready]);if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Finance intelligence</div><h2 className="dashTitle">Platform finance</h2><div className="stats"><div className="stat"><span>Approved sales</span><strong>₹{Number(d.sales||0).toLocaleString("en-IN")}</strong></div><div className="stat"><span>Approved orders</span><strong>{d.salesCount||0}</strong></div><div className="stat"><span>Affiliate earnings credited</span><strong>₹{Number(d.earnings||0).toLocaleString("en-IN")}</strong></div><div className="stat"><span>Payouts marked paid</span><strong>₹{Number(d.payouts||0).toLocaleString("en-IN")}</strong></div></div></div></main>}
