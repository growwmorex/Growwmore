"use client";
import {useEffect,useState} from "react";import AdminNav from "@/components/AdminNav";import {useAdmin} from "@/hooks/useAdmin";import {allDocs} from "@/lib/admin";
export default function Referrals(){const ready=useAdmin(),[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(ready)allDocs("members").then(x=>setRows(x.filter((m:any)=>m.referredBy)))},[ready]);if(!ready)return <main className="shell"><div className="panel">Loading…</div></main>;return <main className="shell"><AdminNav/><div className="dash"><div className="eyebrow">Network ledger</div><h2 className="dashTitle">Referral activity</h2><table className="table"><thead><tr><th>Member</th><th>Referred by</th><th>Status</th><th>Credited basis</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.fullName||x.email}</td><td>{x.referredBy}</td><td><span className="pill">{x.paymentStatus}</span></td><td>{x.paymentStatus==="approved"?"Eligible approved purchase":"Not credited"}</td></tr>)}</tbody></table></div></main>}
