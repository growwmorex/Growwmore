"use client";
import {useEffect,useState} from "react";import {onAuthStateChanged} from "firebase/auth";import {auth} from "@/lib/firebase";import {isAdminEmail} from "@/lib/constants";import {ensureMember} from "@/lib/user";import {useRouter} from "next/navigation";import LuxuryBackground from "@/components/LuxuryBackground";
export default function AdminGateway(){const r=useRouter(),[ready,setReady]=useState(false);useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u||!isAdminEmail(u.email))return r.replace("/login");await ensureMember(u);setReady(true)}),[r]);if(!ready)return <main className="shell center">Verifying founder access…</main>;return <main className="shell"><LuxuryBackground/><div className="gateway"><div className="eyebrow">Founder account detected</div><h1>Choose your workspace.</h1><p>You have complimentary member dashboard access and protected Admin OS access.</p><div className="gatewayGrid"><button className="workspaceCard" onClick={()=>r.push("/dashboard")}><span>Member</span><h2>Open Dashboard</h2><p>Profile, referrals, wallet and withdrawal workspace.</p></button><button className="workspaceCard olive" onClick={()=>r.push("/admin")}><span>Founder</span><h2>Open Admin OS</h2><p>Payments, members, packages, finance and payout control.</p></button></div></div></main>}
