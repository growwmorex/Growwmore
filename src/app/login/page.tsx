"use client";
import Nav from "@/components/Nav";import LuxuryBackground from "@/components/LuxuryBackground";import Link from "next/link";import {useState} from "react";
import {GoogleAuthProvider,signInWithEmailAndPassword,signInWithPopup} from "firebase/auth";import {auth} from "@/lib/firebase";import {useRouter} from "next/navigation";import {friendlyAuthError} from "@/lib/authErrors";import {friendlyFirestoreError,isTransientFirestoreError} from "@/lib/firestoreRetry";import {bootstrapAuthenticatedUser} from "@/lib/authBootstrap";
export default function Login(){const [err,setErr]=useState(""),[busy,setBusy]=useState(false),router=useRouter();
 async function routeUser(user:any){router.replace(await bootstrapAuthenticatedUser(user))}
 async function google(){setBusy(true);setErr("");try{const p=new GoogleAuthProvider();p.setCustomParameters({prompt:"select_account"});const c=await signInWithPopup(auth,p);await routeUser(c.user)}catch(x){setErr(isTransientFirestoreError(x)?friendlyFirestoreError(x):friendlyAuthError(x))}finally{setBusy(false)}}
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr("");const f=new FormData(e.currentTarget);try{const c=await signInWithEmailAndPassword(auth,String(f.get("email")).trim(),String(f.get("password")));await routeUser(c.user)}catch(x){setErr(isTransientFirestoreError(x)?friendlyFirestoreError(x):friendlyAuthError(x))}finally{setBusy(false)}}
 return <main className="shell"><LuxuryBackground/><Nav/><div className="authWrap"><form className="panel authPanel" onSubmit={submit}><div className="eyebrow">Secure member access</div><h1>Welcome back.</h1><p className="sub">Sign in to continue to your member workspace, collections, earnings and referral tools.</p><button type="button" className="googleBtn" onClick={google} disabled={busy}><span>G</span> {busy?"Connecting securely…":"Continue with Google"}</button><div className="divider"><span>or use email</span></div><div className="field"><label>EMAIL</label><input name="email" type="email" required/></div><div className="field"><label>PASSWORD</label><input name="password" type="password" required/></div><div className="authLinks"><Link href="/forgot-password">Forgot password?</Link><Link href="/join">Create account</Link></div>{err&&<div className="error">{err}</div>}<button className="btn wide" disabled={busy}>{busy?"Signing in…":"Sign in →"}</button></form></div></main>}
