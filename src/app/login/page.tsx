"use client";
import Nav from "@/components/Nav";import LuxuryBackground from "@/components/LuxuryBackground";import Link from "next/link";import {useState} from "react";
import {GoogleAuthProvider,signInWithEmailAndPassword,signInWithPopup} from "firebase/auth";import {auth} from "@/lib/firebase";import {getDoc} from "firebase/firestore";import {ensureMember,memberRef} from "@/lib/user";import {withFirestoreRetry,friendlyFirestoreError} from "@/lib/firestoreRetry";import {useRouter} from "next/navigation";import {isAdminEmail} from "@/lib/constants";import {friendlyAuthError} from "@/lib/authErrors";
export default function Login(){const [err,setErr]=useState(""),[busy,setBusy]=useState(false),router=useRouter();
 async function finish(uid:string,email?:string|null){if(isAdminEmail(email))return router.push("/admin-gateway");try{const s=await withFirestoreRetry(()=>getDoc(memberRef(uid)));if(!s.exists())return router.push("/collections");const d=s.data();router.push(d.paymentStatus==="approved"?"/dashboard":d.packageId?"/payment":"/collections")}catch(x){setErr(friendlyFirestoreError(x))}}
 async function google(){setBusy(true);setErr("");try{const provider=new GoogleAuthProvider();provider.setCustomParameters({prompt:"select_account"});const c=await signInWithPopup(auth,provider);await ensureMember(c.user);await finish(c.user.uid,c.user.email)}catch(x){setErr(friendlyAuthError(x))}finally{setBusy(false)}}
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr("");const f=new FormData(e.currentTarget);try{const c=await signInWithEmailAndPassword(auth,String(f.get("email")).trim(),String(f.get("password")));await finish(c.user.uid,c.user.email)}catch(x){setErr(friendlyAuthError(x))}finally{setBusy(false)}}
 return <main className="shell"><LuxuryBackground/><Nav/><div className="authWrap"><form className="panel authPanel" onSubmit={submit}><div className="eyebrow">Secure member access</div><h1>Welcome back.</h1><p className="sub">Sign in to continue to your member workspace, collections, earnings and referral tools.</p><button type="button" className="googleBtn" onClick={google} disabled={busy}><span>G</span> Continue with Google</button><div className="divider"><span>or use email</span></div><div className="field"><label>EMAIL</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>PASSWORD</label><input name="password" type="password" autoComplete="current-password" required/></div><div className="authLinks"><Link href="/forgot-password">Forgot password?</Link><Link href="/join">Create account</Link></div>{err&&<div className="error">{err}</div>}<button className="btn wide" disabled={busy}>{busy?"Signing in…":"Sign in →"}</button></form></div></main>}