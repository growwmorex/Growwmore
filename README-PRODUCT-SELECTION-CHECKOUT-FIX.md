# Growwmore Product Selection + Checkout UI Fix
- New authenticated `/products` page for the five ₹5K/₹8K/₹10K/₹12K/₹15K collections.
- Shopping-style select → floating cart → secure payment flow.
- `/collections` becomes a compatibility redirect to `/products`.
- New users route to `/products`, not homepage.
- Navigation Products link points to `/products`.
- Includes homepage removal note, payment UI note, and CSS block to append to `globals.css`.
